const TestOutline = () => {
  return (
    <div>
      <h1>Test Outline</h1>
      <h3>
        Create a custom react hook to fetch users from an API
        https://randomuser.me/api/ and display their pictures and name
      </h3>
      <h3>
        It must return the list of users, currentUser, function to fetch the
        next user, function to go back to the previous user
      </h3>
    </div>
  );
};

export default TestOutline;
